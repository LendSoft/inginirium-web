# Инжинириум МГТУ — редизайн на Vite + React

Учебный проект. Сверстал две страницы сайта [inginirium.ru](https://inginirium.ru) с нуля, добавил JS-анимации.

---

## Что не так с оригинальным сайтом

- Нет поиска по курсам — 40+ позиций, найти что-то конкретное неудобно
- Форма записи спрятана далеко, нет быстрого способа оставить заявку
- Дизайн устарел, нет анимаций, сайт выглядит статично
- Мобильная версия работает плохо
- У каждого курса нет своей страницы с программой и расписанием
- Статистика ("100 000 учеников") подана как обычный текст — не цепляет
- Нет отзывов встроенных прямо на сайте
- Медленная загрузка, SEO не проработан

---

## Структура проекта

```
src/
├── components/
│   ├── layout/
│   │   ├── Header/        двойной хедер: верхняя строка + основная навигация
│   │   └── Footer/
│   ├── home/
│   │   ├── HeroSlider/    слайдер главного баннера
│   │   ├── StatsSection/  блок с цифрами
│   │   ├── LearningPath/  ступени обучения по возрастам
│   │   └── WhyUs/         блок "почему мы"
│   ├── courses/
│   │   ├── CourseFilter/  кнопки фильтра
│   │   └── CourseGrid/    сетка карточек
│   └── ui/
│       └── CourseCard/    карточка одного курса
├── hooks/
│   ├── useScrollReveal.js
│   └── useCounter.js
├── data/
│   ├── courses.js
│   └── stats.js
└── pages/
    ├── HomePage.jsx
    └── CoursesPage.jsx
```

---

## Анимации

- **Canvas в Hero** — сетка точек с линиями, работает через `requestAnimationFrame`
- **Слайдер** — автопрокрутка каждые 5 секунд, смена слайдов с fade+slide
- **Счётчики** — цифры считаются от 0 до значения когда блок появляется в экране
- **Scroll reveal** — элементы появляются при скролле через `IntersectionObserver`
- **Фильтр курсов** — при смене фильтра карточки исчезают и появляются с задержкой
- **Карточки** — при наведении поднимаются, иконка немного увеличивается и поворачивается
- **Хедер** — тень появляется при скролле вниз
- **Бургер-меню** — иконка анимируется в крестик

---

## Запуск

```bash
npm install
npm run dev
```

Откроется на `http://localhost:5173`
